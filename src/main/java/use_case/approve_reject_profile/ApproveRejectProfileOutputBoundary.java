package use_case.approve_reject_profile;

/**
 * The boundary for the Approve Reject profile use case output
 */
public interface ApproveRejectProfileOutputBoundary {
    void prepareSuccessView(ApproveRejectProfileOutputData outputData);
}
